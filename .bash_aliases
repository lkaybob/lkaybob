alias nv="nvim"

# Mac OS specific aliases
if [[ $(uname -s) == "Darwin" ]] then;
  # common directories
  alias dev="cd $HOME/Desktop/Development"
  alias blog="cd $HOME/Desktop/Development/blog-github"
  
  # typora
  alias typora="open -a Typora"
fi
